# Maskan — Aqar Gap-Fill Roadmap & Session Budget Plan

> Living doc. A new Claude Code session should read **only this file** (not explore the whole repo) to know what's done and what's next. Update the Status table at the end of every session.

---

## Part 1 — Why the Claude usage limit keeps getting hit, and how to stop it

Root causes in how this project has likely been worked on so far:

1. **One giant session covering multiple features.** Each new feature triggers fresh codebase exploration (grepping, reading files) inside the same session, which is the most expensive thing an agent does. Cost compounds instead of resetting.
2. **No persisted plan.** Without a roadmap doc, every session re-derives "what exists, what's missing" from scratch by reading code — that's exactly what the audit above cost.
3. **No stop discipline.** Pushing through to "finish the feature" once a session is already long, instead of stopping at a clean checkpoint and continuing fresh.

Rules to follow going forward:

- **One feature = one session.** Start a new session (or `/clear`) per item in Part 2. Never chain two features in one continuous session.
- **Bootstrap cheap.** At the start of a session, point Claude at this file and the specific files listed under that feature — don't ask it to "explore the codebase" again.
- **Define done before starting.** Use the acceptance criteria listed per feature so the session has a clear stopping point instead of sprawling.
- **Checkpoint and stop.** End every session with: commit the work, update the Status table below, note what's left in "Next step" — even if the feature isn't 100% finished. A half-finished feature with a clear note costs nothing extra next session; a session that runs until it hits the limit mid-thought does.
- **Match model to task.** Reserve heavier reasoning for design/architecture decisions; routine CRUD/UI implementation doesn't need it.
- **Watch `/cost`.** If one feature is burning far more than others, that's a signal to split it into two sessions rather than push through.

---

## Part 2 — Prioritized Aqar gap-fill features (AI-flavored)

Confirmed by codebase audit (2026-08-05): Maskan already has map search, saved-search alerts, and full notification infra — those are **not** gaps. The real gaps vs. Aqar, in priority order:

### 1. Digital Rental Contract Management (Ejar-equivalent)
- **Aqar has:** users manage official Ejar rental contracts digitally inside the app.
- **Maskan gap:** fully absent — no contract/lease model or route exists at all (only a dummy "Contract signed" analytics string).
- **AI flavor:** an AI Contract Assistant that reads the lease terms and flags anything unusual vs. typical Saudi rental norms ("this deposit is 2x the district average," "no maintenance clause found") before the tenant signs — reuses the existing `ai.py` chat infra.
- **Why it fits one session:** new model + route (`backend/app/models/contract.py`, `routes/contracts.py`) + one frontend page; no dependency on other gaps.
- **Acceptance criteria:** tenant can generate a contract from an accepted lead, both parties can view/download it, AI Assistant surfaces at least 3 categories of flags.
- **Backend done (session 1):** `backend/app/models/contract.py` (`Contract` model: lead_id unique FK, tenant_user_id, landlord_mediator_id, property_id, rent_amount, deposit_amount, start_date/end_date, status `draft`→`pending_signature`→`active`→`expired`, separate `tenant_signed_at`/`landlord_signed_at`), migration `f1a2b3c4d5e6_add_contracts.py`, routes in `backend/app/api/routes/contracts.py` mounted at `/api/contracts`:
  - `POST /api/contracts/` — create from an accepted lead (`lead_id`, optional `property_id`, `rent_amount`, `deposit_amount`, `start_date`, `end_date`); caller must be the lead's tenant or its accepted mediator; 409 if a contract already exists for that lead.
  - `GET /api/contracts/my` — contracts where the caller is tenant or landlord mediator.
  - `GET /api/contracts/{id}` — fetch (tenant/landlord/admin only).
  - `POST /api/contracts/{id}/sign` — role-aware signing; flips to `active` once both signed, lazily flips to `expired` past `end_date` on read.
  - **Not yet done:** AI Contract Assistant flags (reuse `ai.py`), frontend page, PDF/download.

### 2. Renter Identity Verification (Nafath-style)
- **Aqar has:** all advertisers verified via Nafath (Saudi national SSO).
- **Maskan gap:** verification exists only for mediators (`Mediator.is_verified` flag, admin-set manually); no renter/tenant identity verification at all.
- **AI flavor:** AI-assisted trust score combining verification status + review history + response rate into a single "Trust Badge," shown next to the existing rental score.
- **Why it fits one session:** can start with a mock/manual verification flow (mirroring the existing mediator pattern) rather than a real Nafath integration, which is a separate infra project.
- **Acceptance criteria:** renter can submit ID for verification, admin can approve (reuse mediator approval UI pattern), verified badge shows on profile/reviews.
- **Done (backend + frontend, one combined session since the earlier "backend done" session prompt was never actually run):**
  - Backend: `User.verification_status`/`is_verified`/`verification_document_ref`/`verification_submitted_at`/`verification_reviewed_at` fields (migration `a1b2c3d4e5f7`), routes in `backend/app/api/routes/verification.py` mounted at `/api/verification` — `POST/GET /me` (renter submit/status), `GET /admin/pending`, `POST /admin/{user_id}/approve`, `POST /admin/{user_id}/reject` (mirrors `mediators.py`'s approve/reject pattern). Exposed on `GET /auth/me` and `UserOut`. `GET /api/users/me/trust-metrics` returns the raw signals (verification, approved review count, lead response rate from `LeadMessage`) for the badge. `Review.reviewer_is_verified` (joined) exposed on `ReviewOut` for the mediator review list.
  - Frontend (mobile only — no renter-facing "profile" screen exists on the web app, only `mobile/app/(tabs)/profile.tsx`): `mobile/src/lib/trustScore.ts` (deterministic weighted formula: 50% verification / 25% review history / 25% response rate → `trusted`/`building`/`new` tier), `mobile/src/components/TrustBadge.tsx` (compact chip + full breakdown card), `mobile/app/verification.tsx` (submit/status screen), wired into the profile screen (badge chip + status row) and the mediator review list (`agent/[id].tsx`, "Verified" chip per reviewer).
  - **Not done:** no admin UI for verification review (API-only, admin can call it directly — same scope call as other admin approval flows in this codebase); no equivalent on the web frontend (renters don't have a profile page there yet).

### 3. Renter-Facing Premium Tier
- **Aqar has:** "Aqar Plus" — paid tier with extra search/filtering/marketing tools.
- **Maskan gap:** subscriptions exist only on the mediator (B2B/landlord) side; nothing for renters.
- **AI flavor:** premium unlocks "AI Alert Plus" — instant (not daily/weekly) AI-summarized alerts on saved searches, and unlimited AI Advisor chat vs. a free-tier message cap.
- **Dependency note (resolved):** the mediator subscription payment gateway integration is now real (`app/core/moyasar.py`, wired into `mediators.py`'s `/me/subscribe` and `/me/renew`), gated by `USE_REAL_PAYMENTS` + `MOYASAR_SECRET_KEY`. **Currently running in mock mode** — no live Moyasar keys are configured in this environment, so the flag defaults off and behavior is unchanged from before. This tier can either flip the flag on (once real keys exist) or knowingly build against the same mock — a conscious choice either way, not a surprise.
- **Why it fits one session:** mostly reuses the existing subscription model/pattern from `Mediator`, applied to `User`.
- **Acceptance criteria:** renter can subscribe, gated features actually check the subscription flag, downgrade/expiry works.

### 4. Short-Term Stay Booking
- **Aqar has:** book short-term stays directly in-app (date-range booking, not just a lead).
- **Maskan gap:** fully absent — platform only models long-term leads, no check-in/check-out or booking concept anywhere.
- **AI flavor:** AI dynamic pricing suggestion for landlords (suggest nightly rate based on area intelligence + season) and an AI availability assistant for renters ("this property is usually booked 2 weeks out").
- **Why it's biggest / split into 2 sessions:** needs a new booking/availability model, calendar UI, and a real payment flow — largest net-new surface area of all five gaps. Session A: data model + backend availability/booking API. Session B: frontend calendar UI + AI pricing/availability features.
- **Acceptance criteria (session A):** booking model with date ranges, conflict prevention at the DB level, API to create/list bookings. (session B): renter can pick dates and book, landlord sees calendar.
- **Backend done (session A):** `backend/app/models/booking.py` (`Booking` model: property_id, renter_user_id, check_in/check_out dates, total_price, status `confirmed`→`cancelled`). Overlap prevention is a Postgres GiST `EXCLUDE` constraint (`ex_bookings_no_overlap`) on `(property_id, daterange(check_in, check_out, '[)'))` restricted to non-cancelled rows — enforced at the DB level even under concurrent writes, requires `btree_gist` extension (created in the migration). Migration `c1d2e3f4a5b6_add_bookings.py`. Routes in `backend/app/api/routes/bookings.py` mounted at `/api/bookings`:
  - `GET /api/bookings/availability?property_id&check_in&check_out` — public, checks the same half-open-range overlap logic the DB constraint uses.
  - `POST /api/bookings/` — auth required; app-level pre-check for a friendly 409, falls back to catching `IntegrityError` from the DB constraint as a second 409 guard against races.
  - `GET /api/bookings/my` — caller's own bookings as renter.
  - `GET /api/bookings/property/{id}` — property's bookings; only the owning mediator or an admin.
  - `POST /api/bookings/{id}/cancel` — renter or admin; cancelling frees the dates (excluded from the overlap constraint via `status <> 'cancelled'`).
  - Tests in `backend/tests/test_bookings.py` (8 tests, all passing against real Postgres — including the actual DB-level overlap rejection, not just app logic).
  - **Not done:** pricing/AI logic, frontend calendar — that's session B (Prompt 9).

### 5. Rent Financing / Pay-Later
- **Aqar has:** "Rent Now, Pay Later" installment financing via partners (Rise, Ijari).
- **Maskan gap:** fully absent — `payment.py` only covers platform-to-mediator billing (subscriptions, lead pickups), not renter financing.
- **AI flavor:** AI affordability advisor — given a renter's stated budget, suggests an installment plan and flags if a property is a stretch vs. their profile.
- **Why lowest priority:** depends on a real payment/financing partner integration (external business relationship), not just code — lower value to build until that's lined up. Do last, or treat as a stub/waitlist feature until a partner is confirmed.
- **Acceptance criteria:** at minimum, a "Request financing" interest-capture flow (no real money movement) that feeds a waitlist — full integration is a separate future project once a partner is chosen.

### Bonus (not an Aqar gap, but relevant to "AI on every feature")
The current **AI Rental Score / Fair Rent Analysis is a client-side heuristic** (`frontend/src/lib/api/maskan.ts:231,267`), not an actual model call — unlike the AI Advisor and Area Intelligence, which are real backend AI. Worth a small session to move scoring server-side through the existing `ai.py` infra so all five "AI-flavored" features are consistently backed by real AI, not a mix of heuristic and real.

---

## Part 2B — New Differentiator Features (beyond Aqar)

Phase 1 (all of Part 2 above) is fully **Done** as of 2026-08-06. User chose to move past Aqar feature-parity into differentiators Aqar doesn't have, rather than closing the minor loose ends noted in Part 2 (mobile financing UI, booking list/cancel UI, persisted nightly rate, search-list AI score, admin review UIs — still open, revisit later if desired). All four features below follow the established `ai.py` pattern: `rate_limit_dependency(...)`, `gateway.run_chat(...)` wrapped in try/except with a `_deterministic_<feature>()` fallback, `finally: gateway.log_ai_call(...)`, response includes `generated_by: "ai"|"fallback"`, prompt registered in `backend/app/core/ai/prompts.py` via `_register(...)` as an UPPER_SNAKE constant.

### 6. AI-Assisted Listing Creation
- **Gap:** landlords write listing titles/descriptions by hand; Aqar doesn't offer AI drafting either — this is a pure differentiator.
- **AI flavor:** landlord enters rough bullet points/keywords, AI drafts a polished title + description + suggested amenity tags, editable before submit.
- **Why one session:** no new DB model — mirrors the existing `pricing-suggestion` endpoint (`ai.py`), which was built and wired to its frontend in a single prior session.

### 7. Real Landlord Analytics + AI Portfolio Insights
- **Gap:** `backend/app/api/routes/analytics.py`'s `/summary` mixes real DB counts (`popular_areas`, `inventory`) with fabricated arithmetic (`search_demand`, `kpis` deltas, `aiTrends`, and a `"Contracts signed"` funnel stage that's actually just `total_users` reused as filler) — `frontend/src/routes/analytics.tsx` seeds hardcoded fallback arrays and silently keeps them on any fetch failure. No endpoint aggregates `Lead`, `Contract`, `Review`, or `Booking` data at all.
- **AI flavor:** a per-mediator "AI Portfolio Insights" narrative — given a landlord's real numbers (lead funnel, contract count, average review rating, booking count, price vs. district average via the existing `_district_avg_monthly_rent` helper), generate a plain-language summary plus 1-2 concrete recommendations.
- **Why split into 2 sessions:** fixing the backend aggregates to be real, plus adding a new AI endpoint, is its own scope; rewiring the dashboard UI to stop trusting hardcoded fallbacks is a separate frontend pass. Session A: real aggregates + AI endpoint. Session B: frontend wiring.
- **Acceptance criteria (A):** `/summary`'s `funnel`, `kpis`, and a new mediator-scoped section reflect real `Lead`/`Contract`/`Review`/`Booking` data, no fabricated arithmetic remains; new AI insights endpoint returns a real narrative. (B): analytics page renders only real data (or an honest "not enough data yet" state), plus the AI Insights panel.

### 8. AI Lease Renewal Assistant
- **Gap:** `Contract.end_date` (`backend/app/models/contract.py:24`) exists but nothing watches it — no renewal reminder of any kind today.
- **AI flavor:** as a contract nears expiry, generate a note comparing current rent to the district's updated average (reusing `_district_avg_monthly_rent`/`AreaIntelligence`) and suggesting renew/renegotiate/vacate, delivered to both tenant and landlord.
- **Why one session:** reuses the existing APScheduler job pattern (`backend/app/jobs/expire_assignments.py`, registered in `main.py`) and the existing generic notification rendering (`frontend/src/lib/notificationDisplay.ts` renders unknown `type` values automatically) — no new frontend surface required, just a new scheduled job + AI note generator + notification creation.
- **Acceptance criteria:** a new job finds contracts expiring within N days (not already reminded — needs a `renewal_reminder_sent_at` field), creates a notification with the AI note for both parties, doesn't re-fire on the same contract.

### 9. AI Roommate / Family-Fit Compatibility Matching
- **Gap:** no user-level preference fields or people-compatibility concept exist anywhere (`User` model has no household/budget/lifestyle fields; the only "match" concept in the codebase is `LeadMatch`/property-search matching, not people-to-people).
- **AI flavor:** renters opt in with household size, budget, lifestyle tags, family status; an AI-generated compatibility explanation (not just a numeric score) is shown alongside candidate matches.
- **Why split into 2 sessions / mobile-only:** needs new `User` fields + matching logic (session A, backend) and a new consumer-facing UI (session B) — renter-facing features live on mobile (`myHome`) per the established web=landlord/mobile=renter split confirmed while building gap #4/#5.
- **Acceptance criteria (A):** renter can set preferences, a matching endpoint returns compatible candidates with an AI (or deterministic-fallback) explanation. (B): mobile screens to set preferences, browse matches, and express interest (a simple notification to the other user is enough — no new chat system needed).

---

## Part 3 — Status tracking

| # | Feature | Status | Session(s) used | Next step |
|---|---|---|---|---|
| 1 | Digital rental contracts + AI assistant | Done | 2 | — |
| 2 | Renter identity verification + AI trust badge | Done | 1 | — |
| 3 | Renter premium tier + AI Alert Plus | Done | 2 | Session 1: `User.subscription_status/tier/started_at/expires_at/moyasar_card_token` + `/api/subscriptions` routes + `get_premium_user` gate (see below). Session 2 (this one): wired the actual "AI Alert Plus" behavior. Backend: `User.is_premium_active` property (point-in-time premium check, safe to call from Celery tasks — unlike `get_premium_user`/`_sync_subscription_expiry` it never mutates/commits). `app/tasks/notifications.py`'s `_create_and_deliver_notification` now only enqueues `enrich_notification_task` (the one-line AI summary via `ai_alert_explainer.py`, which reuses the same `app.core.ai.gateway` as `ai.py`) when `saved_search.user.is_premium_active` — free-tier instant alerts still get the deterministic `rule_based_explanation`, just not the AI one-liner. `ai.py`'s `/chat` and `/chat/stream` gained `_enforce_free_chat_cap`: a per-day Redis counter (`AI_CHAT_FREE_DAILY_LIMIT`, default 15, reuses `check_rate_limit`) scoped by user id (or IP if anonymous), skipped entirely for `is_premium_active` users; hitting it raises 403. Frontend (mobile): new `/premium` upgrade/upsell screen (plan status, benefits, subscribe/renew/cancel wired to `/api/subscriptions/*`), a Premium row + tier badge on the profile screen, `AuthUser`/`fetchMe` now carry `subscription_status`/`subscription_tier`, `AuthProvider.refreshUser()` added to re-sync after subscribing. `advisor.tsx`'s streaming chat detects the cap's 403 (`streamAdvisorChat` special-cases `xhr.status === 403` since the body isn't SSE-shaped) and shows an "Upgrade to Premium" CTA instead of the generic retry bubble. The notification center's existing `meta.ai_explanation` UI (`notifications.tsx`) needed no changes — it already renders whatever `ai.py`/`ai_alert_explainer.py` attaches, so premium-gating the *generation* was the whole gap. No new migration — reused existing subscription columns. Full backend suite green (68 relevant tests); mobile `tsc --noEmit` clean. |
| 4a | Short-term booking — backend | Done | 1 | — |
| 4b | Short-term booking — frontend | Done | 1 | Backend additions (session B): `ai.py` gained `POST /api/ai/pricing-suggestion` (auth required, rate-limited) — takes `{area, city, monthly_rent?}`, returns a nightly rate range for short-term stays. Baseline nightly = `monthly_rent/30` (falls back to the district's average published rent if `monthly_rent` omitted); AI call (new `PRICING_ASSISTANT` prompt in `prompts.py`) adjusts for season (winter Nov-Feb / summer school-break Jun-Jul heuristics, since Ramadan/Eid aren't fixed-Gregorian) and the district's `AreaIntelligence.area_score`, with a deterministic fallback (same season/area multipliers, no AI) used when `ANTHROPIC_API_KEY` is unset or the AI response fails sanity checks. `bookings.py` gained public `GET /api/bookings/property/{id}/insights` — average booking lead time (days between `created_at` and `check_in`) for that property, degrading to a platform-wide average (or a generic message) below a 3-booking sample. New tests: `test_ai_platform.py` (pricing fallback/AI/malformed-response paths) and `test_bookings.py` (insights insufficient-history/averaged/404 paths) — full backend suite green (200 tests). Frontend: added on **both** web and mobile (user chose "both" over a single-platform session). Web (`frontend/src/routes/property.$id.tsx`): new `ShortTermBooking` section (rent listings only) — a `react-day-picker` range Calendar in a Popover for check-in/check-out, availability check via the existing `/bookings/availability`, book via `POST /bookings/`, an insight note from the new insights endpoint, sign-in gating via `useAuth`. Web (`frontend/src/routes/partner.tsx`): AI pricing suggestion box added to `PartnerListingForm` right after the monthly-rent field (mobile has no mediator/listing-edit UI at all, confirmed by exploration, so this stayed web-only). New API client functions in both `frontend/src/lib/api/maskan.ts` and `mobile/src/lib/api/maskan.ts`: `fetchAvailability`, `fetchBookingInsights`, `createBooking`, `fetchMyBookings`/`cancelBooking` (web only, not yet surfaced in any UI), `fetchPricingSuggestion` (web only). Mobile (`mobile/app/property/[id].tsx` + new `mobile/src/components/BookingCalendar.tsx`): a hand-built month-grid range calendar (no calendar/date library exists in the mobile app yet, so one wasn't added) with the same availability-check → book flow and insight note. i18n strings added under `property.booking.*` in all four locale files (web + mobile, en + ar) and `partnerDashboard.listingForm.pricingSuggestion.*` (web only). Known gaps: no per-listing nightly-rate field exists yet — the nightly rate renters see is a client-computed estimate (`monthly_rent/30 * 1.6`), not a value a landlord actually sets; a landlord's AI-suggested rate isn't persisted anywhere, it's advisory only. `tsc --noEmit` clean on both `frontend/` and `mobile/`. |
| 5 | Rent financing (waitlist stub) | Done | 1 | Backend: `backend/app/models/financing_interest.py` (`FinancingInterest`: renter_user_id, property_id, stated_budget, `ai_note`/`ai_generated_by` — the AI note is persisted at submission time so admins can see what the renter was told), migration `d2e3f4a5b6c7_add_financing_interests.py`. Routes in `backend/app/api/routes/financing.py` mounted at `/api/financing`: `POST /` (auth required, rate-limited 10/10min) creates the record and generates the affordability note in the same call; `GET /my` (caller's own); `GET /admin/all` (admin only — no dedicated admin UI page was built, same API-only scope call as verification's admin review in gap #2). AI Affordability Advisor lives in `ai.py` as `generate_affordability_note()` (not its own HTTP endpoint — called directly by `financing.py`, the reverse of `ai.py` importing `_check_contract_access` from `contracts.py`) with a new `AFFORDABILITY_ADVISOR` prompt in `prompts.py`; degrades to a deterministic ratio-based note (`_deterministic_affordability_note`) when `ANTHROPIC_API_KEY` is unset or the AI call fails, same try/except/finally + `log_ai_call` pattern as `contract_flags`/`pricing_suggestion`. Tests in `backend/tests/test_financing.py` (12 tests — deterministic note tiers, AI/fallback/error paths with mocked `gateway.run_chat`, submission/auth/404/validation, my/admin listing) — full backend suite green (212 tests). Frontend: web only (property page = `frontend/src/routes/property.$id.tsx`, which already hosts the short-term booking section from gap #4; no mobile listing-detail equivalent was touched). `ActionsCard` gained a "Request financing" button (rent listings only, `Landmark` icon) opening a new `FinancingModal` (modeled on the existing `ContactModal`): budget input → submit → confirmation screen rendering the AI note in a highlighted box, sign-in gate for guests. New `submitFinancingInterest`/`ApiFinancingInterest` in `frontend/src/lib/api/maskan.ts`. i18n strings added under `property.financing.*` and `property.actions.requestFinancing` (en + ar). `tsc --noEmit` clean. |
| 6 | AI-assisted listing creation | Not started | — | New `POST /api/ai/listing-draft` endpoint + "Draft with AI" button in `partner.tsx` |
| 7a | Real analytics + AI portfolio insights — backend | Not started | — | Replace fabricated fields in `analytics.py`, add mediator-scoped AI insights endpoint |
| 7b | Real analytics + AI portfolio insights — frontend | Not started | — | Depends on 7a |
| 8 | AI lease renewal assistant | Not started | — | New scheduled job + `renewal_reminder_sent_at` field on Contract |
| 9a | Roommate/family-fit matching — backend | Not started | — | New User preference fields + matching endpoint |
| 9b | Roommate/family-fit matching — frontend (mobile) | Not started | — | Depends on 9a |
| 10 | Aqar-style property details page enrichment | Done | 1 | Not an originally-tracked gap — added after the user shared Aqar detail-page screenshots and asked for parity. Backend: migration `e1f2a3b4c5d6_add_property_detail_fields.py` adds to `properties`: `living_rooms`, `property_age_years`, `commission_percent`, 7 boolean feature flags (`has_kitchen`/`has_water`/`has_electricity`/`has_private_roof`/`in_villa`/`has_two_entrances`/`has_separate_electrical_meter`), `license_number`, `license_expiration_date`, `deed_area`, `views_count`, `updated_at`. `properties.py`'s `GET /{id}` now increments `views_count` and attaches `mediator_rating`/`mediator_review_count` (schema-only fields on `PropertyOut`, populated by calling `reviews.py`'s `get_mediator_summary` directly — not stored on `Mediator`, and deliberately not computed on the list endpoint to avoid N+1 queries). New `GET /{id}/similar?limit=6` — same-city, same-area-first, then closest-price, replacing what both frontends used to do by client-side-filtering the entire `/properties/` list. `create_partner_property` defaults `license_number` to the mediator's own if the partner doesn't supply one. `seed.py` backfills all new fields for the 65-listing demo dataset deterministically from each listing's `external_id` suffix (`_derive_detail_fields`). Frontend (both web `frontend/src/routes/property.$id.tsx` and mobile `mobile/app/property/[id].tsx`, plus their `lib/api/maskan.ts` + `lib/maskan-data.ts` type/mapping layers): added Property Features checklist, Rent Payments section, a "Rent now, pay later" banner (web reuses the existing `FinancingModal`; mobile got a new `BottomSheet`-based financing sheet + `submitFinancingInterest` API wrapper since mobile had no financing UI at all before this), a Description section with read-more truncation (this surfaced a pre-existing gap: neither platform's `Property` type carried `description` at all — added it), a "Register your lease contract" banner linking to `/lead/new`, a tabbed/accordion "Listing details" panel (Listing ID, Created At, Views, Deed Area, License Number, License Expiration, Last Updated, Source), mediator star rating + review count on the advertiser card, and real `living_rooms`/`property_age_years`/`furnished` facts replacing web's previously **hardcoded fake** furnishing/building-age/parking values. Comparable/similar listings on both platforms now call the new `/similar` endpoint instead of filtering the full property list client-side. Deleted an unused hardcoded demo `properties` array from `frontend/src/lib/maskan-data.ts` (dead code, not imported anywhere, would have needed updating for the new required `Property` fields). i18n added under `property.summary.*`/`property.features.*`/`property.rentPayments.*`/`property.rentNowPayLater.*`/`property.registerLease.*`/`property.listingDetails.*`/`property.description.*`/`property.landlord.reviews` (web + mobile, en + ar). Verified end-to-end: `tsc --noEmit` clean on both `frontend/` and `mobile/`; web checked visually via a Playwright screenshot against the running dev server (all new sections render with real data, zero console errors); mobile checked visually on a running Android emulator via Metro + deep link (`myhome://property/1`) — all sections including the views counter incrementing live confirmed working. |
| Bonus | Server-side AI rental scoring | Done | 1 | Backend: `ai.py` gained `POST /api/ai/rental-score` (public, optional auth, rate-limited 30/10min) — takes `{listing_type, monthly_rent?, sale_price?, bedrooms?, area, city}`, returns `{score (0-100), reasoning, generated_by, district_avg_monthly_rent, district_area_score}`. Reuses the existing `_district_avg_monthly_rent` helper (rent listings only) and looks up the district's `AreaIntelligence` row (area/school/healthcare/lifestyle/traffic/family scores) for facts fed to a new `RENTAL_SCORE_ASSISTANT` prompt in `prompts.py`; degrades to a deterministic score (`_deterministic_rental_score` — same ratio-vs-district-average bands as the old client-side heuristic, or a bedroom-based estimate for sales/no-rent-data) when `ANTHROPIC_API_KEY` is unset, the AI response is malformed, or the score is out of range — same try/except/finally + `log_ai_call` pattern as `pricing_suggestion`/`contract_flags`. New tests in `test_ai_platform.py` (6 tests: no-auth-required, fallback without key, sale-listing fallback, AI success, malformed-response fallback, out-of-range-score fallback) — full backend suite green (218 tests). Frontend: wired into the property detail page's "Rental Score" AI badge (`frontend/src/routes/property.$id.tsx`'s `RentalIntelligence` component) — the one place in the app where a rental score is actually presented to users as an AI feature (`Sparkles`-badged "Maskan AI Rental Intelligence" / "Rental Score" title in `en.ts`/`ar.ts`), and a natural one-call-per-page-view fit. On mount it calls the new `fetchRentalScore()` (added to `maskan.ts`); on success the badge shows the AI score plus a reasoning line and an "AI-generated score" caption; on failure (network/API unreachable) it silently keeps showing the pre-existing deterministic composite (price fairness + area quality + amenities + commute + family, renamed `fallbackScore`) with an "Estimated score" caption — this deterministic composite is what "keep the old function only as an offline fallback" maps to here, since the property detail page never called the unrelated `estimateRentalScore()` helper in `maskan.ts` (that helper only feeds `SearchProperty.rentalScore`, an internal sort key on the search results list with no visible badge — out of scope for this session). New i18n strings under `rentalIntelligence.aiGenerated`/`estimateGenerated` (en + ar). `tsc --noEmit` clean. |

| 11 | Mobile bottom-nav restructure: Home/Projects/Bookings/Account | Done | 2 | Session 1: not an originally-tracked gap — user shared an Aqar bottom-nav screenshot and asked to move myHome's 5-tab bar toward that shape. `mobile/app/(tabs)/_layout.tsx` restructured to 4 tabs (Home/Projects-placeholder/Bookings-real/Account); Search/Saved/My Leads moved to root-level stack screens. See prior note (kept below) for full session-1 detail. Session 2 (this one): built the actual Projects feature end-to-end, closing the "not done" gap from session 1. User shared Aqar-style project-detail screenshots ("maher 10" — gallery, developer card, unit-count/area/price summary, description, Introducing Document link, nearby-landmarks map, unit card, "Nearby projects" rail) and asked for a real listing screen plus working "Nearby projects" navigation (previously those cards didn't exist at all, let alone link anywhere). Backend: new `Project`/`ProjectUnit`/`ProjectImage` models (`backend/app/models/project.py`, mirrors `Property`/`ListingImage` — denormalized `developer_name`/`developer_logo_url` on `Project` instead of a `Mediator` FK, since a developer brand isn't the same as a licensed rental/sale agent), migration `f2a3b4c5d6e7_add_projects.py` (head was `e1f2a3b4c5d6`), schemas in `backend/app/schemas/project.py`, read-only routes in `backend/app/api/routes/projects.py` (`GET /`, `GET /{id}` with `views_count` increment, `GET /{id}/similar` — same-city/closest-price, mirrors `properties.py`'s `/similar`), registered in `main.py`'s `_ROUTERS`. No write endpoints yet — no partner/admin UI exists to create projects, matching session 1's mobile-only nav scope. Seed: `backend/seed_projects.py` (5 demo Riyadh projects across Al Yasmin/Al Malqa/Al Olaya — districts already covered by `DISTRICT_COORDS`/`AreaIntelligence` seed data so the map pin and nearby-landmarks section have real data; one project, "Maher 10," modeled directly on the user's screenshot for visual comparison). Tests: `backend/tests/test_projects.py` (list/filter/detail/views-increment/404/similar-ordering — the similar-ordering test needed `limit=20` since it runs against the shared local DB alongside the 5 real seeded projects, not an isolated fixture set); full suite 224 passed. Mobile: `ApiProject`/`ApiProjectUnit`/`ApiProjectImage` types + `fetchProjects`/`fetchProject`/`fetchSimilarProjects`/`mapApiProject` added to `mobile/src/lib/api/maskan.ts`; UI `Project`/`ProjectUnit` types in `maskan-data.ts`; new `ProjectCard.tsx` (reuses the generic `Badge` component for category/completion/featured chips — no save/call/WhatsApp actions, unlike `PropertyCard`, since projects aren't saved or called directly) and `ProjectSimilarListings.tsx` (mirrors `PropertySimilarListings.tsx`) components. `mobile/app/(tabs)/projects.tsx` now a real `useFetch`-backed listing screen (skeleton/error/empty states, `FlatList` of `ProjectCard`), replacing the "coming soon" placeholder — `projects.comingSoon.*` i18n keys removed, replaced with `projects.list.*`/`projects.detail.*`. New `mobile/app/project/[id].tsx` detail screen, registered in `app/_layout.tsx`'s root `Stack` — reuses `PropertyImageGallery`, `PropertyLocationMap`, and `Accordion` as-is (all already generic/prop-driven, no property-specific coupling to work around); "Nearby Landmarks" reuses the existing `AreaIntelligence` endpoint (`fetchAreaIntelligence`) rather than a new landmarks table, rendering `schools`/`hospitals` with `distance_km`, hidden entirely when empty. Contact CTA deep-links to the existing `/lead/new` screen with the project's city/district and a note pre-filled via new optional `useLocalSearchParams` support added to `lead/new.tsx` (backward compatible — investigated attaching to `Lead` directly first, but `Lead`'s creation/matching pipeline is built around area+budget matching with no `project_id` concept, so a prefilled deep-link was the correct-weight solution rather than distorting that pipeline). `tsc --noEmit` clean. Verified end-to-end on a running Android emulator (Metro reload, not a native rebuild — JS-only change): Projects tab lists all 5 seeded projects with real prices/badges/images; tapping into "Malqa Business Residences" renders gallery, developer card, unit/area/price summary, description with read-more, Know More accordion with real nearby-landmarks data, location map, per-unit cards, and a "Nearby Projects" rail that included "Maher 10"; tapping that card navigated to Maher 10's own detail page (the original bug — "Nearby projects" cards previously led nowhere since the screen/model didn't exist); tapping "Contact" correctly landed on `/lead/new` with City "Riyadh", District "Al Yasmin", and note "Interested in project: Maher 10" pre-filled. (Caught and fixed along the way: two stale backend `uvicorn` processes were fighting over port 8000 from a prior session — the new `/api/projects` route 404'd until both were killed and the server restarted from the correct venv, per `main.py`'s no-hot-reload note.) No web-side changes (web is the landlord/mediator surface; Projects is mobile/renter-facing only, consistent with session 1's split). Prior session-1 detail (unchanged, kept for history): `mobile/app/(tabs)/_layout.tsx` now has exactly 4 tabs — Home (`index`), Projects (was placeholder, now real per above), Bookings (new, real), Account (`profile`, unchanged file). Search/Saved/My Leads screens moved out of the tab group into root-level stack screens (`mobile/app/search.tsx`, `saved.tsx`, `leads.tsx` — same route paths since Expo Router group folders don't affect the URL, so this was a pure directory move + adding each screen's own `<Stack.Screen options={{title}}>` since they no longer inherit title from `Tabs.Screen`), registered in `mobile/app/_layout.tsx`'s root `Stack`, and linked from three new rows on the Account screen (`profile.tsx`: Search always visible, Saved + My Leads inside the existing `user &&` gated block). Fixed 4 stale `/(tabs)/search` references (`saved.tsx`, `saved-searches.tsx` x2, `estimate.tsx`) to plain `/search`. Bookings tab (`mobile/app/(tabs)/bookings.tsx`) is fully real: added `fetchMyBookings`/`cancelBooking` to `mobile/src/lib/api/maskan.ts`, built a list screen following the `leads.tsx`/`property-requests/index.tsx` pattern. |

| 12 | Bookings tab: real browse/search + Reserve flow with sample listings | Done | 1 | Not an originally-tracked gap — same session pattern as row 11/Projects: user shared Aqar booking-flow screenshots (date-range + city search header, "N units match your search", listing cards with nightly price, a detail page with Property Information/Features, "Unit Rules" — arrival/departure time, latest booking time, insurance amount — a location map, "Similar ads nearby," and a sticky Reserve bar) and asked for "the booking functionality as for Projects" plus sample listings. Unlike Projects, most of the backing infrastructure already existed and worked (`Booking` model with a DB-level overlap exclusion constraint, `GET /bookings/availability`, `GET /bookings/property/{id}/insights`, `POST /bookings/`, `GET /bookings/my`, `POST /bookings/{id}/cancel`, and a full inline reserve widget `mobile/src/components/BookingCalendar.tsx` already wired into `mobile/app/property/[id].tsx`) — confirmed by direct inspection before planning, not assumed. The actual gaps were narrower: no discovery/browse entry point (every published rent `Property` was implicitly bookable via its own detail page, with no distinct flag or way to search across them), no real per-listing nightly rate (previously a `monthly_rent/30*1.6` client heuristic only), and no Unit Rules/elevator/AC fields or sample bookable listings. Backend: extended `Property` (not a new table, since `Booking` already FKs to `Property`) with `is_bookable`, `nightly_rate`, `has_elevator`, `has_airconditioners`, `arrival_time`, `departure_time`, `latest_booking_time`, `insurance_amount` — migration `a3b4c5d6e7f8_add_booking_fields_to_properties.py` (head was `f2a3b4c5d6e7`). `GET /properties/` (existing endpoint, not a new one) gained `is_bookable` and `check_in`/`check_out` filters, the latter excluding properties with an overlapping non-cancelled `Booking` via a `NOT EXISTS` subquery mirroring `bookings.py`'s `_overlap_filter` — this is what makes date-filtered search results real rather than decorative. Seed: `backend/seed_bookable_listings.py` (4 demo Riyadh listings — "Apartment for booking" titles/nightly rates modeled on the user's screenshots, e.g. 1,240 SAR/night — across Al Qirawan/Al Yasmin/Al Malqa/Al Olaya). Tests: `backend/tests/test_properties.py` gained `is_bookable`-filter and date-range-exclusion tests; full suite 226 passed. Mobile: `ApiProperty`/`mapApiProperty`/UI `Property` type extended with the 8 new fields (all optional, harmless defaults for the pre-existing 145 properties); new `fetchBookableProperties()` (reuses `GET /properties/` + `requestJsonWithCount`, not a separate endpoint). `elevator`/`airconditioners` deliberately placed inside the existing `PropertyFeatures` sub-object (not flat fields) so `property/[id].tsx`'s existing generic `PropertyFeaturesList` loop picks them up with no new rendering logic. `BookingCalendar.tsx` gained an optional `nightlyRate` prop — prefers the real rate when present, falls back to the existing heuristic when null (one-line change, backward compatible with every existing call site). New `BookableListingCard.tsx` (nightly price instead of annual rent, links to the existing `/property/[id]` route — no separate booking-detail screen). **User confirmed via question**: the Bookings tab becomes the browse/search screen (matching the Aqar screenshots) rather than staying as "My Bookings," which moved to a new `mobile/app/my-bookings.tsx` (relocated unchanged, registered in root `Stack`, reachable via a "My Bookings" link button on the new browse screen) — same relocation pattern row 11 used for Search/Saved/My Leads. `mobile/app/(tabs)/bookings.tsx` rewritten: date-range button (opens a `BottomSheet` with a month-grid range picker adapted from `BookingCalendar`'s pattern) + city filter (`SelectField`/`OptionModal`, reusing `lead/new.tsx`'s pattern) + "My Bookings" link + `useFetch`-backed `FlatList` of `BookableListingCard`s. `property/[id].tsx` gained a new "Unit Rules" section (arrival/departure/latest-booking time, insurance amount) rendered only when `property.isBookable` — non-bookable properties are visually unchanged, verified directly (property #1, a regular villa, shows no Elevator/Airconditioners chips and no Unit Rules section, while still using the heuristic nightly rate for its own `BookingCalendar`). `tsc --noEmit` clean. Verified end-to-end on the Android emulator: Bookings tab lists all 4 seeded listings with real nightly prices and specs; date-range sheet opens, selects a range, and re-fetches (confirmed "Aug 7 - Aug 8" → "Aug 11 - Aug 13" with results updating); tapping a card opens `property/[id]` showing the real nightly rate in "Book a Short Stay," Elevator/Airconditioners in Property Features, and the new Unit Rules block; "My Bookings" link navigates to the relocated screen. (Caught and fixed along the way: two stale backend `uvicorn` processes on port 8000 again, same as row 11 — killed and restarted from the venv before the new `is_bookable` filter would respond; also discovered mid-session that RN's LogBox dev-warning indicator can visually overlap and intercept taps on the bottom tab bar — confirmed pre-existing/unrelated to this session's code by reproducing it on an untouched regular property page, not a regression.) No web-side changes (web is the landlord/mediator surface; consistent with rows 11/Projects). |
| 13 | Web-side Projects: details page with Call/WhatsApp, partner project listings, admin moderation | Done | 1 | Not an originally-tracked gap — row 11 built Projects mobile-only (explicitly "no web-side changes... web is the landlord/mediator surface"); user now asked for the web side: clicking a project opens a details page with Call/WhatsApp buttons like the property details page, and partners must attach a call number + WhatsApp number when listing a project, "including DB schema." Confirmed by direct inspection before planning: web had **zero** Projects surface (no routes, no API client code) — this was a from-scratch web build, not additive. Also confirmed `Project` had no `mediator_id`/ownership link and no moderation gate at all (every row implicitly public), unlike `Property`'s Draft/Pending Approval/Published/Suspended/Rejected workflow — asked the user directly whether partner-submitted projects should get the same approval gate as properties; **user chose yes**, which added an admin moderation UI to scope (not just a details-page feature). Backend: migration `c5d6e7f8a9b0_add_partner_fields_to_projects.py` (head was `b4c5d6e7f8a9`, the same-day-uncommitted Property `contact_phone`/`whatsapp_phone` migration found already in the working tree) adds `mediator_id` FK, `contact_phone`/`whatsapp_phone`, and a **new** `listing_status` column — deliberately not a repurposing of the existing `status` column, which already means "Available"/"Sold Out" (an availability badge rendered on mobile) and had to keep meaning that; `listing_status` defaults to `Published` so all pre-existing/admin-seeded rows are unaffected. `Project` model gained `call_phone`/`whatsapp_number` computed properties mirroring `Property`'s exactly. New partner routes `GET /projects/partner/mine`, `POST /projects/partner/`, `PATCH /projects/partner/{id}` (mirrors `properties.py`'s partner routes: `get_mediator_user` auth, forces `listing_status="Pending Approval"`, geocodes via `coords_for`, re-submits for approval on edit) plus admin `POST /projects/`, `PATCH /projects/{id}`. Public `GET /projects/`, `GET /{id}`, `GET /{id}/similar` now filter to `listing_status == "Published"` unless the caller is an admin passing `include_all=true` (mirrors `list_properties`'s `get_optional_admin_user` pattern) — verified backward-compatible for mobile's existing unauthenticated calls since all pre-existing rows default to Published. Deliberately skipped: outbox events for Project create/update (`record_event` calls Property makes feed a notification pipeline — `app/tasks/notifications.py`, saved-search alerts — with zero Project-aware consumers; wiring that up is a separate, unrequested feature) and per-project image-gallery endpoints (partner form uses a single `image_url` cover field, not the `ProjectImage` multi-image gallery, to keep scope to what was asked). Frontend web: `ApiProject`/`ApiProjectUnit`/`ApiProjectImage` types + `mapApiProject`/`fetchProjects`/`fetchProject`/`fetchSimilarProjects`/partner+admin CRUD functions added to `frontend/src/lib/api/maskan.ts`; new UI `Project`/`ProjectUnit` types in `maskan-data.ts`; new `frontend/src/routes/projects.tsx` (list) and `project.$id.tsx` (details) — details page's Call/WhatsApp card is a leaner sibling of `property.$id.tsx`'s `LandlordCard`/`PhoneModal` (same `whatsappLink()`/`ContactButtons` reuse from `components/maskan/ContactButtons.tsx`), not that exact component reused directly, since `LandlordCard` assumes an agent profile with ratings/a `/agent/$id` link that doesn't fit a developer project. New `nav.projects` link in `TopNav.tsx`. Partner portal: `partner.tsx` gained a third `"projects"` `PartnerView` alongside existing `"leads"`/`"listings"`, with `PartnerProjectsView`/`PartnerProjectForm` mirroring `PartnerListingsView`/`PartnerListingForm` — the `contact_phone`/`whatsapp_phone`/`whatsappSameAsCall` form block was copied verbatim (entity-agnostic), and the "can only edit once already Published" gate (`listing_status === "Published"`) mirrors Property's same rule exactly. Admin: `admin.tsx` gained a `"projects"` `AdminView` with a new `ProjectsModerationView` — deliberately a scoped-down mirror of the full Listings admin panel (no bulk multi-select, no image manager, no create-form modal, since admins moderate rather than author project content), Publish/Reject/Suspend buttons calling the new `PATCH /projects/{id}`. Both `en.ts`/`ar.ts` extended in lockstep (the dict is a `TranslationDict` structural type — a key added to one without the other fails `tsc`), reusing existing `partnerDashboard.listingForm.*`/`listingsView.statusBadges.*` keys wherever the copy was entity-agnostic rather than duplicating strings. Verified: `alembic upgrade head` against local Postgres (existing 5 seeded projects came back with `listing_status='Published'` unaffected); `tsc --noEmit` and `vite build` both clean; `eslint` clean on every new/touched file (pre-existing CRLF/prettier drift and two unrelated `no-unused-expressions` errors elsewhere in `admin.tsx` confirmed via `git diff` to predate this session); backend smoke-tested live (`GET /projects/` returns real seeded data with the new fields, unknown-project 404s); both dev servers started and `/projects`/`/project/1`/`/partner`/`/admin` all returned clean 200 HTML with no error-boundary text. Could not interactively click through Call/WhatsApp buttons or submit the partner form in a real browser — no browser-automation tool available in this environment; recommended the user do a manual click-through. |

Update this table at the end of every session, even for partial progress.
